{
  "name": "swift-agency-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Swift Agency static frontend for Vercel deployment",
  "scripts": {
    "dev": "serve -l 8000 .",
    "start": "serve -s -l 8080 .",
    "build": "echo 'Static site — no build step'"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
